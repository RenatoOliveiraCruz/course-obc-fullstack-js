let speed = 80;

while (speed > 0) {
    alert("A velocidade atual é: " + speed + " km/h");
    speed -= 20;
    alert("Reduzindo 20 km/h");

    if (speed = 40) {
        break;
    };
};

alert("O veículo parou!");